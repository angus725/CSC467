{
int identifier = expression;

const int anotherID = 5;


if (expression)
	variable = expression;
else
	while (expression)
	{
		if (expression)
			if (expression)
				some_var = some_exp;
			else
				some_var = some_exp;
	}
	if(n || x >= true)
	{
		bool x = true;
		vec2 y = vec2(50); /*vec2(50,5); causes a lexer error*/
		m = (5.5 + 3);
		x = !x;
		y = x[5];
	}
	
	c = dp3(asdasd);
	
}
